# PawPals / PAWPAIR 実装タスク台帳

最終更新: 2026-09-22（JST）。確定仕様の調査基点: `feat/pawpals-local-integration@6609e68`。
今回の実装完了は [受入条件・検証証拠対応表](docs/integration/pawpals-acceptance.md) の各行で判定する。旧デモの完了記録を今回機能へ引き継がない。

## 今回の確定仕様と担当

| ID | 範囲・受入条件 | 担当 | 状態 | 依存・次の一手 |
|---|---|---|---|---|
| P01 | モック構造化質問・7軸を保存/AI/Rules/validation/同意へ一貫して接続。自由記述互換 | backend・UI・[09] | 検証済み（保存・表示） | `d12a381` / `87ad6c0` / `d6dd66c`。7軸の相性点利用はP05 |
| P02 | 日付付き当日対象をスタッフが明示選択・保存し、選択犬だけ計算 | backend・UI・[09] | 検証済み | `3350193` / `87ad6c0` / `d6dd66c`。Rules/E2E成功 |
| P03 | 施設別部屋設定の永続化・読み戻し・計算への使用 | backend・UI・[09] | 検証済み | `3350193` / `87ad6c0` / `d6dd66c`。Rules/E2E成功 |
| P04 | 最新案のみ承認。選択スタッフ、4状態遷移、旧案superseded、理由と元案参照 | backend・UI・[09] | 検証済み | `3350193` / `87ad6c0` / `d6dd66c`。理由付き承認・監査E2E成功 |
| P05 | 既存信号とAI生成7軸を相性100点へ統合。適用状態/内訳、hard制約、実際のAI根拠を表示。未確定5分類追加禁止 | matching・UI・[09] | 検証済み | `9b727cb` / `b1cb969`。7軸最大18点、欠損時既存計算、hard block維持 |
| P06 | 承認/却下/再計算の監査と最新未確定案だけの待ち件数 | backend・UI・[09] | 検証済み | `3350193` / `87ad6c0` / `d6dd66c`。監査E2E成功 |
| P07 | 手動観測の実動線、将来operation event/observation ingestion型・adapter・テスト | backend・UI・[09] | 検証済み（カメラIF範囲） | `373f39e` / `87ad6c0` / `d6dd66c`。cameraはunsupported |
| P08 / [06] | 設計書・ER・アーキテクチャ・台帳・証拠表を同期 | 文書担当[06] | 検証済み（文書の範囲のみ） | 文書専用commitをpushし[09]/[EX]へ共有。実装証拠は後続 |
| P09 | 実装統合・全体受入・最終レビュー | [09]・全体レビュー担当 | 検証済み | 最終統合 `b1cb969`、19ファイル127件、typecheck/build/Rules/E2E成功 |
| P10 | 今回変更の公開配備・実環境疎通 | 配備担当 | 配備済み（認証後E2Eを除く） | Hosting/Rules/Indexes deploy成功。公開asset/ログイン/Worker health確認 |

進行中は担当割当と作業依頼済みを表す。基点に新実装が入ったことや検証完了を意味しない。各担当のpush SHA・実行結果を受領してから状態を更新する。

## 状態と証拠

- **未着手**: その工程の開始・完了証拠を未受領。コードが存在しないと断定する意味ではない。
- **進行中**: 担当作業中、またはコードはあるが受入条件の証拠が揃っていない。
- **検証済み**: 実行commit、環境、手順、結果、証拠が揃い、該当条件を満たす。
- **配備済み**: 対象commitを指定環境へ配備し、対象URL・Rules/Workerを含む疎通証拠がある。

## 取り込み順・所有権

1. backendの型/Rules/データ契約を別commitで共有する。
2. UI担当がその契約を用いたフォーム・スタッフ画面を別commitで共有する。
3. [09]だけが `src/App.tsx` を統合し、実保存・読み戻し・再計算・承認・監査を接続する。
4. matching担当が7軸を含む100点計算、version、内訳、旧データ経路を実装し、[09]が同じ結果を保存・最適化・表示へ接続する。
5. 文書commitは独立して取り込み可能。記載基点と未検証状態を保持し、実装取り込み後に証拠表を更新する。
6. 統合後に要件表に沿う受入検証と `gpt-5.6-sol / high` の全体レビューを行う。

担当タスク:
- backend: `01a0c655-2bf7-78b3-93c3-96189d2077e7`
- UI: `01a0c6e1-709d-7c60-9e90-5be65d12a9b6`
- App統合[09]: `01a0c65a-931a-7633-9753-8135ff14d945`

以後の実装・調査・文書・統合・レビューは最新指定の `gpt-5.6-sol / high`。担当境界と既存差分を保持し、隔離worktree・専用リモートブランチを用いる。main直接push/force pushは禁止。人間判断は[BB]、検証済み結果は[EX]へ。[AA]へタスク間連絡を送らない。

## 既存検証記録（今回要件とは別）

| 範囲 | 記録 | 制限 |
|---|---|---|
| 旧公開デモ | 2026-09-22、架空2頭登録→Worker/OrcaRouter→Firestore→全1ペア→1部屋→観測再計算→確定。アプリ34/34、Worker15/15、typecheck/build/qa-preflight成功との旧台帳記録 | 旧認証なし構成の履歴。今回仕様の証拠にはしない |
| モック統合基点 | アプリ46件、Worker15件、typecheck/build、Rules emulator、ローカルブラウザE2Eを記録 | [詳細・制限](docs/integration/pawpals-mapping.md)。公開配備なし |
| 基点ブラウザ登録 | Auth/Firestore emulator + AI test double。別途テキスト1件の公開Worker/実OrcaRouter疎通 | 新7軸・今回監査・今回当日対象を検証していない。実媒体操作と実機QR未確認 |
| App統合前の中間記録 | backend統合 `d12a381` / `3350193` / `373f39e`、UI `87ad6c0`。アプリ18ファイル114件、Worker18件成功 | 後続I03でtypecheck/build/E2E完了。公開配備の証拠にはしない |
| App統合 | `d6dd66c`。typecheck、18ファイル114件、build、Rules実emulator、招待から承認監査までのブラウザE2E、desktop/mobile、console error/warn 0 | AI生成7軸を相性100点へ使う追加要件は別担当の成果待ち。公開配備は別 |
| 最終統合・配備 | `9b727cb` / `b1cb969`。19ファイル127件、相性96.3%、7軸16.3/18点、軸別内訳、390px、console 0。Hosting/Rules/Indexes deploy成功 | 公開側の認証後E2Eは本番資格情報がないため未実施 |

旧公開Hosting: `https://pawpair-ai-hack-2026.web.app`。旧Worker: `https://pet-hotel-agent-api.nosea41oceanv.workers.dev`。URLの存在を最新仕様の配備証拠にしない。無料枠のみ、秘密情報を露出させない。

## [06]文書同期の検証・委任記録

- 変更は文書7ファイルのみ。実装コード・公開設定・既存生成画像は変更していない。
- 相対リンク20件のファイル存在とコードフェンス4組、差分空白を確認。Mermaidの関連を読んで、部屋設定が施設ごとに1文書であること、案がない当日対象/部屋設定の監査も表せることを確認した。Mermaidレンダラーでの描画は未実施。
- `/root/mock_contract_scan`（default role、`gpt-6-astra / high`）へモック項目照合と文書レビューを委任。実モック由来の18項目/6問/7軸を独立に確認するため。モデルと強度は今回の明示指定に従った。
- アプリ/Worker/Rulesのテストとbuildは未実施。今回は文書のみの更新であり、追加機能の実装検証は各担当と[09]が証拠表へ記録する。
