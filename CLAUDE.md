# Git 共同運用ルール

このリポジトリでは、人間と AI エージェントの双方が以下を必ず守る。既存の作業を保護し、判断に迷う場合は操作を止めて担当者へ確認する。

## タスクごとのリモート共有

- このリポジトリのタスクで作成・更新したコミットは、タスク専用のリモートブランチへ push して共有する。完了報告前に push 成功を確認し、ブランチ名と commit SHA を報告する。
- ユーザーがそのタスクで「pushしない」と明示した場合は、その指示を優先する。認証・権限・リモート競合などで安全に push できない場合は、未pushの事実と原因を報告し、ユーザーへ判断を戻す。
- 他者と共有中のブランチへ作業を混ぜない。タスクブランチがない場合は `feat/`、`fix/`、`docs/`、`chore/` の命名規則で作成し、他者の未コミット変更を含めない。
- push はタスクブランチに限る。`main` へ直接 push/merge しない。force push は行わない。
- push 前に fetch して対象ブランチとの差分を確認し、ブランチ先端が進んでいたら安全に統合できるか判断する。判断できない競合では停止して報告する。
- 作業が読み取り専用でコミットを作らない場合、またはユーザーが変更・pushを禁じた場合はpush不要。

## 作業開始前

1. `git status --short --branch`、`git branch --show-current`、`git remote -v` を実行し、リポジトリ、ブランチ、未コミット変更を確認する。
2. `git fetch --prune origin` を実行し、`git log --oneline --decorate --graph --all -n 20` と `git diff` でリモートとの差分およびローカル変更を確認する。
3. 1 タスクにつき 1 ブランチを作り、可能なら専用 worktree を使う。`main` では直接編集・コミット・push しない。
4. 他の作業者と同じファイルを変更する場合は、編集前に担当範囲と変更箇所を調整する。担当外の変更には触れない。

ブランチ名は `feat/<短い目的>`、`fix/<短い目的>`、`docs/<短い目的>`、`chore/<短い目的>` のいずれかとし、英小文字とハイフンを基本にする。

## 編集とコミット

- 他のメンバーや AI エージェントの未コミット変更を上書き、削除、巻き戻ししない。意図不明の差分は保持し、担当者へ確認する。
- コミットは小さく、目的を 1 つに絞る。無関係な整形、リネーム、生成物、担当外の変更を混ぜない。
- コミット前に `git status --short` と `git diff` を確認し、対象ファイルだけを明示して stage する。`git add .` や `git add -A` は、全差分を確認済みの場合を除き使わない。
- コミットメッセージは `feat: ...`、`fix: ...`、`docs: ...`、`test: ...`、`chore: ...` の形式で、変更の目的を簡潔に記す。
- 秘密情報、実値入りの `.env`、API キー、認証情報、秘密鍵、個人情報をコミットしない。`.env.example` にはダミー値だけを置く。
- 大容量ファイル、ビルド成果物、キャッシュ、ログなど再生成可能な生成物をコミットしない。必要なら先に `.gitignore` を更新する。
- 依存関係を変える場合は、マニフェストと対応するロックファイルを同じコミットに含める。ロックファイルを手編集せず、採用済みのパッケージ管理ツールで再生成し、意図しない大量更新がないか確認する。

## 禁止操作

次の操作は原則として実行しない。例外が必要なら、対象・理由・影響を示してリポジトリ管理者の明示承認を得る。

- `main` または他者と共有中のブランチへの force push（`--force`、`--force-with-lease`）
- 共有ブランチの rebase や履歴改変
- `git reset --hard`
- `git checkout -- <path>`、`git restore <path>`、`git clean` などによる未コミット変更の破棄
- 他者のファイル、ブランチ、タグ、worktree の無断削除
- 内容を確認していない一括 stage、commit、push

## 同期、コンフリクト、push

1. push 前に `git fetch --prune origin` を実行し、`git status --short --branch`、`git diff --check`、`git diff origin/main...HEAD`、コミット一覧を確認する。
2. リモート更新を取り込む際は、自分専用の未共有ブランチでのみ rebase を検討する。共有済みなら merge を用い、履歴を書き換えない。
3. コンフリクトでは両者の変更意図を確認して統合し、片側を機械的に採用しない。意図を判断できなければ解消を中断して担当者へ相談する。
4. プロジェクトで定めたテスト、lint、型チェック、ビルドを実行する。手順が未定なら、少なくとも変更内容の構文・リンク・差分を確認する。
5. push 後に PR を作成し、`main` へ直接マージしない。PR には目的、主な変更、確認・テスト結果、影響範囲、未解決事項、関連 Issue（あれば）を記載する。タイトルは変更目的が分かる短い文にする。

## 完了条件

以下をすべて満たしたときだけ完了とする。

- `git status --short` で意図しない変更がない。
- `git diff --check` が成功し、`git diff origin/main...HEAD` がタスク範囲だけを含む。
- 必要なテストが成功し、失敗または未実施項目を PR に明記した。
- 秘密情報、不要な生成物、無関係な変更が含まれていない。
- ユーザーが禁止していない限り、タスクブランチを push し、push 成功を確認した。
- PR はリポジトリの運用またはユーザー依頼で必要な場合に作成する。`main` へ直接マージしない。
